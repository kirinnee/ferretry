# Attachment subsystem survey

Source examined: `/home/kirin/.config/home-manager/modules/kteam-ts/src/attachments.ts`,
`pdf-decrypt.ts`, `pdf-decrypt-worker.ts`, `session-manager.ts`, and `api-server.ts`.

| Source capability                                       | Ferretry status              | Ferretry path / gap                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Durable attachment originals and upload/download routes | PORTED                       | `packages/daemon/src/lib/attachments/session-attachments.ts`, `packages/daemon/src/lib/runtime/mounts/session-attachments.ts`                                                                                                                                                                                                                                         |
| Daemon-keyed attachment identity                        | PORTED                       | The repository key is `(daemonId, sessionId, attachmentId)` and its root is derived from the opened daemon state home in `packages/daemon/bin/fyd.ts`.                                                                                                                                                                                                                |
| Encrypted PDF unlock in a disposable worker             | PORTED                       | `decrypt-pdf.ts` and `decrypt-pdf-worker.ts`; qpdf's virtual filesystem, password, and intermediate plaintext die with the worker.                                                                                                                                                                                                                                    |
| Plaintext never reaches host storage                    | PORTED                       | Only `original` and `manifest.json` are persisted. `session-attachments.test.ts` asserts the decrypted marker is absent from every stored file.                                                                                                                                                                                                                       |
| Password handling                                       | PORTED WITH DELIBERATE LIMIT | The password is request-only and use-once: it is parsed without echoing request values, passed to the worker, then neither cached nor journaled. This follows the secret subsystem's no-projection/redaction boundary; the secret vault is not used because it deliberately has no read API and retaining an attachment password would violate the use-once decision. |
| Unlock state cleanup when a session ends                | GAP                          | `SessionAttachmentStore.releaseSession` zeroes cache entries, but the lifecycle close/remove paths do not invoke it yet. The daemon must not claim cleanup is guaranteed until that hook is mounted.                                                                                                                                                                  |
| PWA lock prompt mounting                                | GAP                          | `attachment-unlock-prompt.tsx` and pending-strip controls remain unmounted by the chat page.                                                                                                                                                                                                                                                                          |
| Agent attachment injection                              | GAP                          | Sends may carry durable attachment IDs now that routes exist, but no session-turn delivery mechanism gives a decrypted in-memory byte stream to an agent yet.                                                                                                                                                                                                         |

The predecessor's key finding is preserved here: `FileSessionAttachmentStore` writes every planned
byte into the session directory, so it cannot represent an in-memory-only decrypted attachment and
must not be used by the unlock path.

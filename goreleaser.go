// Package main is a placeholder compiled by GoReleaser's Go builder.
//
// The CLI is a Bun binary, not a Go program. GoReleaser still needs a "build" to hang its
// archive/cask/release pipeline on, so it compiles this trivial stub into each target slot; the
// post-build hook (scripts/release/goreleaser-shim.sh) then swaps the real prebuilt Bun binary
// in over the stub. This file is never shipped.
package main

func main() {}
